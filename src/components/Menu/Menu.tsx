import { HomeButton, MenuContainer, MenuWrapper } from './_menu';

const Menu = () => {
  return (
    <MenuWrapper>
      <HomeButton>
        <img src="/assets/imgs/logo.png" alt="" />
      </HomeButton>
      <MenuContainer>Menu em construção </MenuContainer>
    </MenuWrapper>
  );
};

export default Menu;
